# Проверка версии 09.09.2026

- `go test ./...` — прошёл.
- `go vet ./...` — прошёл.
- `go test -race ./...` — прошёл.
- `node --check frontend/v2-broadsheet.js` — прошёл.
- `bash -n scripts/*.sh` — прошёл.
- Сборка трёх Linux amd64 программ с `CGO_ENABLED=0` — прошла.
- Содержимое архива проверено: программы, исходники, assets, конфигурация; без живой БД и учётных данных.
- Живая RSS-загрузка из Sports.ru и «Чемпионата» проверена: оба источника вернули новости; повторные циклы обновляют ленту. В локальном окружении использовался ограниченный режим синтетического DNS, описанный в README.
- Сквозная HTTP-проверка админки: вход, multipart-загрузка PNG, отображение баннера/ERID в HTML, выдача картинки, форма редактирования, удаление. Тестовый баннер удалён.
- В браузере проверены: главная, фильтр хоккея, поиск «Кросби», сохранение/список сохранённого, страница новости и ссылка на источник.
- Проверены широкая и узкая версии. На узком экране поле поиска доступно, горизонтального переполнения страницы нет. В консоли проверенных страниц ошибок нет.

На VPS заказчика сервисы не запускались: для этого нужны установка архива, реальный домен/HTTPS и собственный администратор. Табло внешнего спортивного API в эту версию не входит. HTML5/ZIP-креативы не поддерживаются; загружаются PNG/JPEG/GIF. Свежая выдача содержит последние 200 записей, более старые новости доступны по ранее сохранённым ссылкам из базы.
